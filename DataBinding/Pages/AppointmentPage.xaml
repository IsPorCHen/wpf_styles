<Page x:Class="DataBinding.Pages.AppointmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="AppointmentPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Информация о пациенте" Style="{StaticResource SubHeaderTextBlock}"/>
            <WrapPanel Style="{StaticResource VerticalWrapPanel}">
                <TextBlock Text="Фамилия:"/>
                <TextBlock Text="{Binding CurrentPatient.LastName}"/>
                <TextBlock Text="Имя:"/>
                <TextBlock Text="{Binding CurrentPatient.Name}"/>
                <TextBlock Text="Отчество:"/>
                <TextBlock Text="{Binding CurrentPatient.MiddleName}"/>
                <TextBlock Text="Дата рождения:"/>
                <TextBlock Text="{Binding CurrentPatient.Birthday}"/>
                <TextBlock Text="Телефон:"/>
                <TextBlock Text="{Binding CurrentPatient.PhoneNumber}"/>
            </WrapPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Style="{StaticResource StackPanelMarginRight}">
                <TextBlock Text="Прием пациента" Style="{StaticResource SubHeaderTextBlock}"/>
                <WrapPanel Style="{StaticResource VerticalWrapPanel}">
                    <Label Content="Дата приема"/>
                    <DatePicker Style="{StaticResource DatePicker200}" SelectedDate="{Binding AppointmentDate, Mode=TwoWay}"/>
                    <Label Content="Диагноз"/>
                    <TextBox Style="{StaticResource MultilineTextBox40}" Text="{Binding DiagnosisText, Mode=TwoWay}"/>
                    <Label Content="Рекомендации"/>
                    <TextBox Style="{StaticResource MultilineTextBox60}" Text="{Binding RecommendationsText, Mode=TwoWay}"/>
                    <Button Click="SaveAppointmentButton_Click" Content="Сохранить прием" Style="{StaticResource Button180}"/>
                </WrapPanel>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="История приемов" Style="{StaticResource SubHeaderTextBlock}"/>
                <ListView ItemsSource="{Binding CurrentPatient.AppointmentStories}" Style="{StaticResource ListView300}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <StackPanel Style="{StaticResource HorizontalStackPanel}">
                                    <TextBlock Text="Дата: "/>
                                    <TextBlock Text="{Binding Date}"/>
                                </StackPanel>
                                <StackPanel Style="{StaticResource HorizontalStackPanel}">
                                    <TextBlock Text="Диагноз: "/>
                                    <TextBlock Text="{Binding Diagnosis}"/>
                                </StackPanel>
                                <StackPanel Style="{StaticResource HorizontalStackPanel}">
                                    <TextBlock Text="Рекомендации: "/>
                                    <TextBlock Text="{Binding Recommendations}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Style="{StaticResource HorizontalStackPanel}">
            <Button Click="BackButton_Click" Content="Назад" Style="{StaticResource Button100}"/>
        </StackPanel>
    </Grid>
</Page>